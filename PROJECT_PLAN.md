# Vue 날씨 과제 구현 계획

## 1. 문서 목적

이 문서는 교수님이 제공한 `skala-vue-main` 소스를 바탕으로 날씨 과제를 어떤 순서로 구현할지 정리한 작업 기록이다. 교수님 필수 요구사항과 직접 추가할 기능을 구분하고, 구현 중에 생긴 문제와 해결 과정을 빠뜨리지 않고 남기는 데 목적이 있다.

아직 구현하지 않은 내용은 완료한 것처럼 기록하지 않는다. 실제로 확인한 문제, 수정 이유, 결과를 구현 단계마다 이 문서와 `README.md`에 추가한다.

## 2. 작업 원칙

- 교수님 원본 프로젝트 `/Users/mk/projects/skala-vue-main`은 수정하지 않는다.
- 과제용 복사본인 현재 프로젝트에서만 작업한다.
- 교수님이 작성한 `WeatherMockup.vue`를 1단계 출발점으로 사용한다.
- 한 단계가 끝나면 완성된 파일을 다음 단계 파일로 복사한 후 기능을 확장한다.
- 단계별 기능 차이가 보이도록 이전 단계 파일은 남긴다.
- API 키는 코드에 직접 작성하지 않고 환경변수로 관리한다.
- 코드는 처음 보는 사람도 데이터 흐름을 이해할 수 있도록 핵심 로직에 한국어 주석을 작성한다.
- 주석은 모든 줄을 설명하기보다 상태가 필요한 이유, 이벤트 흐름, 데이터 가공 기준, 예외 처리 이유를 설명한다.

## 3. 단계별 파일 확장 방식

```text
WeatherMockup.vue
    -> 복사 후 Composition API 기능 추가
WeatherComposition.vue
    -> 복사 후 컴포넌트 분리
WeatherParent.vue 및 하위 컴포넌트
    -> Router의 메인 View로 확장
WeatherHomeView.vue / WeatherDetailView.vue
    -> Pinia 적용
    -> Axios와 실제 날씨 API 적용
최종 날씨 애플리케이션
```

파일 이름을 변경하여 이전 단계를 없애지 않고, 복사한 뒤 다음 단계에서 수정한다.

## 4. 최종 앱 콘셉트

현재 날씨를 조회하는 기능에 그치지 않고, 시간별 날씨 데이터를 이용해 생활에 필요한 정보를 보여주는 날씨 대시보드를 만든다.

### 개인 기능

1. 현재 날씨와 시간대에 따라 바뀌는 동적 날씨 장면
   - 맑음: 햇살과 빛줄기
   - 흐림: 움직이는 먹구름
   - 비: 떨어지는 빗방울
   - 눈: 흩날리는 눈송이
   - 태풍·뇌우: 어두운 배경, 빠른 구름, 번개
   - 밤: 달과 별이 보이는 야간 테마
2. 산책·빨래 생활 추천
   - 산책: 기온, 강우 확률, 미세먼지, 초미세먼지를 종합한다.
   - 빨래: 강우 확률, 습도, 날씨 상태를 종합한다.
3. 위험기상 경고
   - 비, 눈, 태풍 또는 뇌우 예보가 있으면 눈에 띄는 경고를 표시한다.
4. 현재 위치 날씨
   - 브라우저 위치 권한으로 좌표를 받은 후 현재 지역의 날씨를 조회한다.

시간별 날씨, 강우 확률, 미세먼지, 초미세먼지, 습도는 각각 별도 기능으로 세지 않고 상세 화면과 생활 추천을 계산하기 위한 데이터로 사용한다.

## 5. 단계별 배치

| 단계 | 교수님 필수 구현 | 개인 기능 배치 |
| --- | --- | --- |
| 1. Mockup | `v-for`, `v-if`, 입력·클릭 이벤트, 날씨 배열 | 개인 도시 데이터와 Mock 날씨에 따른 장면 변경 |
| 2. Composition | `ref`, `computed`, `watch`, `watchEffect`, 검색 필터 | 날씨 테마와 산책·빨래 추천 계산 |
| 3. Component | `props`, `emit`, `slot`, 컴포넌트 분리 | 생활 추천 카드와 위험기상 경고 컴포넌트 |
| 4. Router | 메인·상세·소개·404, `/weather/:cityId` | 현재 위치 View와 `/my-location` 경로 |
| 5. Pinia | 섭씨·화씨 `configStore` | 현재 위치를 관리하는 `locationStore` |
| 6. Axios | 실제 OpenWeather 데이터 적용 | Open-Meteo 시간별 예보·대기질과 현재 위치 데이터 연결 |
| 7. UI Library | Element Plus 적용 | 날씨 장면, 경고, 시간별 예보, 추천 카드 UI 완성 |
| 8. 배포 | ESLint, 환경변수, 빌드·배포 | README, 화면 캡처, History, Troubleshooting 정리 |

## 6. 단계별 구현 내용

### 1단계: Weather Mockup

- 날씨 배열을 `ref`로 작성한다.
- `v-for`와 `:key`로 개인 도시 카드와 날씨 입자를 반복 출력한다.
- `v-if`로 선택한 도시의 `theme`에 필요한 햇살·구름·비·눈·번개만 조건부 렌더링한다.
- `@input`으로 검색어를 저장하고 `@click`으로 선택 도시와 날씨 장면을 함께 변경한다.
- 카드 내부 버튼에는 `@click.stop`을 사용해 부모 카드의 선택 이벤트와 분리한다.
- 교수님 예제 외에 개인 도시 데이터를 추가한다.
- 실제 API 없이 Mock 데이터의 날씨 상태로 기본 장면을 변경한다.
- README에는 반복 렌더링에 `v-for`, 조건부 장면에 `v-if`, 사용자 행동과 상태 연결에 이벤트가 적합하다고 판단한 이유를 기록한다.

### 2단계: Composition API

- 검색어, 선택 도시, 날씨 목록을 반응형 상태로 관리한다.
- `computed`로 검색 목록과 현재 날씨 테마를 계산한다.
- `computed`로 산책과 빨래 추천 결과를 계산한다.
- `watch`로 선택 도시의 이전 값과 새 값을 비교해 장면·추천 갱신 안내를 표시한다.
- `watchEffect`로 검색어와 계산된 목록 개수를 함께 추적해 검색 상태 문구를 표시한다.
- 콘솔 확인에 그치지 않고 네 가지 Composition API 기술을 개인 기능의 화면 동작에 연결한다.

### 3단계: Component

- 교수님 요구 구조인 `WeatherParent`, `BaseDashboardCard`, `SearchBar`, `WeatherCard`를 유지한다.
- `LifeRecommendationCard.vue`를 추가해 산책·빨래 추천을 표시한다.
- `WeatherAlertBanner.vue`를 추가해 위험기상 경고를 표시한다.
- 날씨 장면은 별도 컴포넌트로 분리하여 콘텐츠 클릭을 방해하지 않게 한다.
- 장면·경고·추천 데이터는 `props`, 검색·선택·상세 이벤트는 `emit`, 공통 카드 내부는 `slot`으로 연결한다.

### 4단계: Router

- 메인, 도시 상세, 서비스 소개, 404 페이지를 구성한다.
- 도시 카드를 누르면 `/weather/:cityId`로 이동한다.
- 시간별 날씨와 강우 확률은 도시 상세 페이지에 표시한다.
- 개인 View인 `MyLocationView.vue`와 `/my-location` 경로를 추가한다.
- `/weather/:cityId`의 params로 같은 상세 View를 도시별로 재사용하고 주소를 직접 열어도 같은 도시를 찾게 한다.

### 5단계: Pinia

- 교수님 필수 `configStore`로 섭씨와 화씨를 관리한다.
- 개인 `locationStore`에 좌표, 권한 진행 상태, 오류 상태를 저장한다.
- 위치의 state, 좌표 존재 여부 getter, 권한 요청 action을 Store로 묶어 Route 이동 뒤에도 상태가 유지되게 한다.

### 6단계: Axios와 실제 API

- Axios 서비스에서 OpenWeather 현재 날씨·대기질과 Open-Meteo 시간별 예보·대기질을 요청한다.
- API 키는 `VITE_OPENWEATHER_API_KEY` 환경변수로 분리하고 키가 없으면 Open-Meteo 실제 데이터로 동작한다.
- 시간별 기온, 강우 확률, 습도, PM10, PM2.5를 기존 추천 데이터 형식으로 변환한다.
- 비·눈·뇌우·강풍 조건을 경고 컴포넌트에 전달한다.
- 현재 위치 좌표도 같은 Axios 서비스 함수로 요청한다.
- 로딩, 통신 실패, 위치 권한 거부, 일부 도시 실패와 다시 불러오기를 처리한다.

### 7단계: UI Library와 CSS

- Element Plus의 Input, Card, Button, Alert, Skeleton, Empty, Table, Tag를 개인 기능의 실제 상태에 사용한다.
- 필요한 컴포넌트만 `main.js`에 등록해 전체 라이브러리 등록으로 인한 번들 증가를 줄인다.
- 날씨 효과 레이어에 `pointer-events: none`을 적용해 버튼과 검색창을 방해하지 않게 한다.
- 지나치게 많은 빗방울과 눈송이를 만들지 않고 성능을 확인한다.
- `prefers-reduced-motion` 사용자는 애니메이션을 줄이거나 정지한다.
- 모바일 화면에서도 카드와 시간별 예보가 잘 보이게 한다.

### 8단계: 품질관리와 배포

- ESLint 전체 검사에서 발견한 교수님 연습 파일의 미사용 import와 catch 변수를 정리해 오류와 경고가 없는 상태를 확인했다.
- API 키를 `.env`로 분리하고 `.gitignore`로 제출 대상에서 제외했다.
- 빌드 명령을 개발 모드가 아닌 기본 프로덕션 빌드로 수정했다.
- Vue DevTools를 개발 서버에서만 등록하여 배포 빌드에서 제외했다.
- GitHub Actions에 ESLint, 빌드, GitHub Pages 배포 과정을 작성했다.
- 상세 Route에서 새로고침해도 화면을 복구할 수 있게 빌드 후 `404.html`을 생성하도록 했다.
- 실제 GitHub 저장소 업로드와 배포 주소 확인은 GitHub 로그인 후 진행한다.

## 7. 예상 컴포넌트 구조

```text
src/
├── components/
│   └── exercise/
│       ├── WeatherMockup.vue
│       ├── WeatherComposition.vue
│       ├── WeatherParent.vue
│       ├── BaseDashboardCard.vue
│       ├── SearchBar.vue
│       ├── WeatherCard.vue
│       ├── LifeRecommendationCard.vue
│       ├── WeatherAlertBanner.vue
│       └── weather-effects/
│           ├── WeatherScene.vue
│           ├── SunnyEffect.vue
│           ├── CloudyEffect.vue
│           ├── RainEffect.vue
│           ├── SnowEffect.vue
│           └── StormEffect.vue
├── views/
│   ├── WeatherHomeView.vue
│   ├── WeatherDetailView.vue
│   ├── WeatherAboutView.vue
│   ├── MyLocationView.vue
│   └── NotFoundView.vue
└── stores/
    ├── configStore.js
    └── locationStore.js
```

컴포넌트 수는 구현하면서 조정한다. 단순한 효과까지 억지로 파일로 나눠 코드 탐색이 어려워지면 `WeatherScene.vue` 하나로 합친다.

## 8. README 작성 계획

`README.md`는 구현이 끝난 뒤 한 번에 작성하지 않는다. 단계가 끝날 때마다 다음 내용을 추가한다.

- 이번 단계에서 구현한 교수님 필수 기능
- 직접 추가하거나 변경한 기능
- 어떤 이벤트에 어떤 데이터를 적용했는지
- 수업에서 배운 내용을 실제 코드에 적용한 이유
- 발생한 문제, 원인 추정, 수정 내용, 수정 후 결과
- 다음 단계로 확장하면서 바뀐 구조
- 주요 화면 캡처

### 이벤트와 데이터 흐름 기록 후보

- 검색창 `@input` -> `searchQuery` 변경 -> `computed` 검색 결과 갱신
- 날씨 카드 `@click` -> 도시 ID 전달 -> 상세 Route 이동
- 상세 버튼 `@click.stop` -> 카드 클릭 이벤트 전파 방지
- 위치 버튼 `@click` -> 브라우저 좌표 요청 -> 좌표 기반 API 호출
- API 응답 변경 -> 날씨 장면과 생활 추천 결과 재계산

## 9. Troubleshooting 기록표

아래 표에는 실제로 발생하고 확인한 문제만 기록한다.

| 날짜 | 단계 | 문제 현상 | 원인 | 수정 내용 | 확인 결과 |
| --- | --- | --- | --- | --- | --- |
| 2026-08-25 | 1단계 Mockup | 데스크톱에서 앱이 화면 왼쪽 절반에만 표시됨 | Vue 기본 템플릿의 `#app`에 2열 Grid가 남아 있었음 | `main.css`의 데스크톱 레이아웃을 단일 열로 바꾸고 앱 컨테이너를 가운데 정렬함 | 1280px 화면에서 앱이 900px 너비로 중앙에 표시되는 것을 확인함 |
| 2026-08-25 | 1단계 Mockup | Mockup만 실행했는데도 빌드 파일이 약 1MB로 생성됨 | `main.js`에 이후 단계에서 사용할 Router, Pinia, Element Plus가 모두 등록되어 있었음 | 1단계에서는 Vue 앱만 등록하고 외부 플러그인은 해당 단계에서 추가하도록 정리함 | JS 빌드 파일이 약 1,003KB에서 64KB로 줄어든 것을 확인함 |
| 2026-08-25 | 2단계 Composition | `watch`와 `watchEffect`가 콘솔 출력만 하고 개인 기능의 화면에는 사용되지 않음 | 문법 실행 여부 확인에 집중해 상태 변경 뒤 필요한 사용자 기능을 정하지 않았음 | `watch`는 도시 변경 안내에, `watchEffect`는 검색 결과 안내에 연결함 | 제주 검색 시 1건 안내, 도시 선택 시 변경 문구·태풍 경고·추천 갱신을 확인함 |
| 2026-08-25 | 6단계 Axios | 교수님 예제의 API 키가 컴포넌트 문자열에 직접 작성되어 있음 | 예제는 통신 구조 설명에 집중해 환경변수 분리가 적용되지 않았음 | 키를 `VITE_OPENWEATHER_API_KEY`로 분리하고 `.env`를 ignore함 | 키가 없는 환경에서 401 요청 없이 Open-Meteo 실제 데이터가 표시됨 |
| 2026-08-25 | 6단계 Axios | 날씨와 대기질 API 응답을 기존 추천 함수에 바로 넣을 수 없음 | 두 API가 시간별 값을 서로 다른 배열과 필드명으로 반환함 | 시간 문자열로 두 응답을 합쳐 기존 시간별 객체 형식으로 변환함 | 제주 상세에서 12시간 예보·PM 수치·산책 및 빨래 추천이 함께 표시됨 |
| 2026-08-25 | 6단계 Axios | 위치 권한 완료 시점을 View가 알 수 없음 | 기존 Pinia action이 콜백 결과를 반환하지 않았음 | action이 `Promise<boolean>`을 반환하고 성공한 경우에만 Axios를 호출하도록 변경함 | 권한 요청 전에는 API가 실행되지 않고 위치 화면이 대기 상태로 표시됨 |
| 2026-08-25 | 7단계 UI Library | Element Plus 적용 후 메인 JS가 약 1,057KB가 되어 큰 청크 경고가 발생함 | `app.use(ElementPlus)`로 사용하지 않는 컴포넌트까지 전체 등록함 | 실제 사용하는 아홉 종류의 컴포넌트만 `main.js`에 등록함 | 메인 JS가 약 392KB로 줄고 500KB 청크 경고가 사라짐 |

최종 README에는 위 기록에서 의미 있는 사례를 최소 5개 이상 골라 학생의 말투로 자세히 정리한다.

## 10. 현재 결정 History

### 2026-08-27

- OpenWeather 본인 API 키를 `.env`에 추가했고 키 값이 Git 대상에 포함되지 않는 설정을 확인했다.
- 제출에 GitHub 저장소 링크와 배포 링크가 필요할 것으로 판단해 GitHub Pages 자동 배포 Workflow를 추가했다.
- 기존 빌드 명령의 `--mode development`를 제거하고 Vue DevTools를 개발 서버에서만 사용하도록 정리했다.
- GitHub Pages 하위 경로와 History Route 새로고침을 고려해 저장소 이름 `base` 전달과 `404.html` 생성 과정을 추가했다.
- GitHub 로그인 후 저장소를 생성하고, Actions 실행·배포 주소·상세 Route 새로고침을 실제로 확인하기로 했다.

### 2026-08-25

- 교수님 소스 전체를 새 과제 폴더로 복사하고 원본은 보존하기로 했다.
- `WeatherMockup.vue`부터 시작하고 단계가 끝날 때마다 다음 파일로 복사해 확장하기로 했다.
- `node_modules`와 임시 파일은 복사하지 않고 `npm ci`로 의존성을 다시 설치했다.
- 실행 진입점인 `App.vue`에서 1단계 `WeatherMockup.vue`만 보이도록 연결했다.
- 초기 빌드는 성공했다. ESLint에서는 현재 과제와 무관한 교수님 연습 파일 두 곳의 미사용 변수 경고가 확인되어 최종 정리 단계에서 처리하기로 했다.
- 개인 도시 데이터로 제주, 인천, 광주를 추가했다.
- 도시 카드를 선택하면 Mock 데이터의 `theme` 값에 따라 맑음, 비, 흐림, 태풍, 눈 장면이 변경되도록 구현했다.
- 맑음의 햇살, 흐림의 이동 구름, 비의 빗방울, 눈의 눈송이, 태풍의 먹구름·비·번개 효과를 브라우저에서 확인했다.
- 검색창 입력값 표시와 상세보기 버튼의 이벤트 전파 방지 동작을 확인했다.
- 1단계를 다시 점검하면서 필수 문법을 단순 예제로 두지 않고, `v-for`는 개인 도시·날씨 입자 반복에, `v-if`는 날씨별 장면 선택에, 입력·클릭 이벤트는 검색어 저장과 장면 전환에 사용한 이유를 README에 기록했다.
- 1단계에서는 Vue 기본 문법에 집중할 수 있도록 `main.js`에서 Router, Pinia, Element Plus 등록을 잠시 제외했다.
- 완성된 `WeatherMockup.vue`를 `WeatherComposition.vue`로 복사한 뒤 2단계 기능을 추가했다.
- `computed`로 도시 검색 결과, 날씨 장면 클래스, 선택 도시의 시간별 예보, 산책 추천, 빨래 추천을 계산했다.
- 2단계 재점검에서 콘솔 출력만 하던 `watch`와 `watchEffect`를 각각 도시 변경 안내와 검색 결과 안내를 만드는 화면 기능으로 수정했다.
- 검색 결과 없음, 제주 태풍 시 추천 불가, 서울의 산책·빨래 추천 시간이 올바르게 표시되는 것을 브라우저에서 확인했다.
- 완성된 `WeatherComposition.vue`를 `WeatherParent.vue`로 복사한 뒤 3단계 컴포넌트 분리를 진행했다.
- 검색창과 날씨 카드는 `props`와 `emit`으로 부모와 통신하고, 공통 카드에는 `slot`으로 내용을 주입했다.
- 개인 컴포넌트로 `WeatherScene`, `LifeRecommendationCard`, `WeatherAlertBanner`를 추가했다.
- 제주를 선택했을 때 태풍 장면, 위험기상 경고, 추천 불가 문구가 함께 표시되고 콘솔 오류가 없는 것을 확인했다.
- 4단계에서 `App.vue`를 Navigation Bar와 `RouterView` 구조로 변경하고 Router를 `main.js`에 등록했다.
- 메인, 도시 상세, 소개, 현재 위치, 404 View를 연결하고 `/weather/:cityId` 동적 경로를 확인했다.
- 제주 상세 화면에서 시간별 날씨·강우 확률·습도·미세먼지·초미세먼지와 태풍 경고가 표시되는 것을 확인했다.
- 개인 View인 `/my-location`은 권한 요청 UI까지 구현하고 실제 날씨 호출은 Axios 단계로 남겼다.
- 5단계에서 `configStore`의 섭씨·화씨 상태를 메인 카드, 상세 헤더, 시간별 예보에 적용했다.
- 개인 `locationStore`를 추가해 좌표, 권한 진행 상태, 오류 상태를 View와 분리했다.
- 화씨로 변경한 뒤 Route를 이동해도 Pinia 상태가 유지되고 28℃가 82℉, 22℃가 72℉로 표시되는 것을 확인했다.
- 2~5단계 필수 기술이 개인 기능의 실제 동작에 사용되는지 다시 확인하고, 각 단계 README에 기술의 의미와 선택 이유를 `개인 기능에 대한 설명` 소제목으로 기록했다.
- 최근 검색 저장 기능은 구현 범위에서 제외했다.
- 미세먼지·초미세먼지는 수치와 상태만 보여주고 행동요령은 제외했다.
- 산책과 빨래 추천을 하나의 생활 추천 기능으로 묶었다.
- 단순 배경색 변경 대신 햇살, 구름, 비, 눈, 태풍·뇌우를 표현하는 동적 날씨 장면을 구현하기로 했다.
- 6단계에서 Open-Meteo 실제 예보와 대기질을 연결하고 OpenWeather는 개인 환경변수 키가 있을 때 함께 사용하도록 구성했다.
- 여섯 도시 요청은 `Promise.allSettled`, 한 도시의 예보·대기질 요청은 `Promise.all`로 처리했다.
- 실제 API 응답을 기존 데이터 구조로 변환해 날씨 장면과 산책·빨래 추천이 실제 값으로 다시 계산되게 했다.
- 제주 상세에서 앞으로 12시간의 기온·강우·습도와 PM10·PM2.5가 표시되고 섭씨 28℃가 화씨 82℉로 바뀌는 것을 확인했다.
- 현재 위치 Pinia action의 완료 결과를 기다린 뒤 좌표 기반 Axios 요청을 실행하도록 연결했다.
- 7단계에서 검색, 카드, 버튼, 위험기상·API 안내, 로딩, 빈 결과, 시간별 표에 Element Plus를 적용했다.
- 날씨 장면과 추천은 기존 개인 CSS를 유지하고 Element Plus 카드의 body 영역만 반투명 스타일로 조정했다.
- 전체 Element Plus 등록 시 메인 JS가 약 1,057KB가 되어 사용 컴포넌트만 등록했고 약 392KB로 줄였다.
- 브라우저에서 도시 검색의 빈 상태, 제주 상세 이동, 12행 시간별 Table, 단위 변경, 현재 위치 버튼을 확인했다.

## 11. 구현 전 확인할 사항

- 개인 도시 데이터에 넣을 도시
- 실제 날씨·시간별 예보·대기질·위험기상 데이터에 사용할 API 범위
- 태풍·기상 특보 데이터 제공 여부와 사용 조건
- 최종 서비스 이름과 대표 색상
- 수업 자료에서 README에 연결할 개념과 표현
